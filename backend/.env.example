PORT=3000
PUBLIC_BASE_URL=http://localhost:3000

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=change-me
DB_NAME=htp_web_dev

JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=7d

SKETCHBOOK_LEGACY_ROOT=/absolute/path/to/uploads/sketchbook/legacy
SKETCHBOOK_CURRENT_ROOT=/absolute/path/to/uploads/sketchbook/current

# Real LLM calls are disabled unless explicitly enabled.
RUN_REAL_LLM_TEST=0
OPENAI_API_KEY=
OPENAI_PREMIUM_REPORT_MAX_OUTPUT_TOKENS=10000
